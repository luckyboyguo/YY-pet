- 修复家长端飞书同步弹窗交互：测试连接按钮不再触发弹窗关闭。
- 修复创建表格逻辑：创建前会读取当前输入框配置，避免使用旧配置导致权限误报。
- 飞书同步配置已预置 appToken/tableId/appId/appSecret，并支持自动刷新 access token。
- 本地启动了静态服务用于验证：admin.html 可通过 http://127.0.0.1:18081/admin.html 访问。
- 定位飞书连接失败根因：浏览器直连 open.feishu.cn 被 CORS/预检拦截，非 token 本身错误。
- 新增 local-server.js：静态资源 + /api/feishu 反向代理，前端改为代理优先。
- admin 端新增详细报错展示（显示 lastError），并固定测试连接/创建表格按钮 type=button。
